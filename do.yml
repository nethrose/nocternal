name: nocternal
region: nyc
services:
- name: nocternal
  type: "static-site"
  environment_slug: "static-site"
  output_dir: build
  github:
    branch: "main"
    deploy_on_push: true
    repo: "nethrose/nocternal"
  output_dir: "build"
  routes:
    - path: "/"
      type: "http"
      port: 80
